# Apply only to a separate experimental dsh-v0.1.2-alpha.4 Web profile.
# The host must resolve the local @hypermemetic-ai/qq-ui-alpha4-spike package.
- insert:
    - id: qq-ui-alpha4-spike
      name: '@hypermemetic-ai/qq-ui-alpha4-spike'
